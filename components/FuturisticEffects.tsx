'use client';

// This component is now essentially empty since we're not adding any dynamic effects
export default function FuturisticEffects() {
  // We don't actually render or do anything now - all styling comes from CSS
  return null;
}
